const DailyForecast = () => {
  return (
    <div>
      <ul>
        {/* {trips.map((trip) => (
          <li key={trip.id}></li>
        ))} */}
      </ul>
    </div>
  );
};

export default DailyForecast;
